# 🔄 МИГРАЦИЯ: Включение старых пользователей в систему напоминаний video1

## 🎯 Цель

Включить существующих пользователей, застрявших на `video1`, в систему автоматических напоминаний через 10 минут.

## ⚠️ ВАЖНО ПОНЯТЬ

### Без миграции:
- ❌ Старые пользователи (до деплоя) НЕ получат напоминание
- ✅ Новые пользователи (после деплоя) получат через 10 минут

### С миграцией:
- ✅ Старые пользователи получат напоминание
- ✅ Новые пользователи получат напоминание (как обычно)

## 📊 Что произойдет

### Сценарий A: Пользователь застрял >10 минут назад
```
lastActivityAt = 2 ноября 10:00
Сейчас = 2 ноября 15:00
Прошло = 5 часов

После миграции:
video1ShownAt = 2 ноября 10:00
→ При следующей проверке (через 1 мин) получит напоминание
```

### Сценарий B: Пользователь застрял <10 минут назад
```
lastActivityAt = 2 ноября 14:58
Сейчас = 2 ноября 15:00
Прошло = 2 минуты

После миграции:
video1ShownAt = 2 ноября 14:58
→ Получит напоминание в 15:08 (через 8 минут)
```

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Откройте Railway PostgreSQL Dashboard

1. Перейдите на https://railway.app/
2. Откройте ваш проект
3. Кликните на **PostgreSQL**
4. Перейдите в **Data** → **Query**

### Шаг 2: Проверьте количество пользователей

Скопируйте и выполните:

```sql
SELECT 
  COUNT(*) as total_users_to_update,
  COUNT(CASE WHEN "lastActivityAt" <= NOW() - INTERVAL '10 minutes' THEN 1 END) as will_get_reminder_immediately,
  COUNT(CASE WHEN "lastActivityAt" > NOW() - INTERVAL '10 minutes' THEN 1 END) as will_get_reminder_later
FROM users 
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ShownAt" IS NULL;
```

**Результат покажет:**
- `total_users_to_update` - всего будет обновлено
- `will_get_reminder_immediately` - получат напоминание сразу (застряли >10 мин)
- `will_get_reminder_later` - получат позже (застряли <10 мин)

**Пример:**
```
total_users_to_update | will_get_reminder_immediately | will_get_reminder_later
---------------------|-------------------------------|------------------------
        32           |              28               |           4
```

### Шаг 3: Посмотрите примеры пользователей

```sql
SELECT 
  "userId",
  username,
  "lastActivityAt",
  EXTRACT(EPOCH FROM (NOW() - "lastActivityAt"))/60 as minutes_since_last_activity,
  CASE 
    WHEN "lastActivityAt" <= NOW() - INTERVAL '10 minutes' THEN 'Получит напоминание СРАЗУ'
    ELSE 'Получит позже'
  END as reminder_timing
FROM users 
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ShownAt" IS NULL
ORDER BY "lastActivityAt" DESC
LIMIT 20;
```

**Пример результата:**
```
userId    | username  | minutes_since_last_activity | reminder_timing
----------|-----------|----------------------------|------------------
123456789 | john_doe  |           320.5            | Получит СРАЗУ
987654321 | jane_doe  |            5.2             | Получит позже
```

### Шаг 4: ПРИМЕНИТЬ МИГРАЦИЮ ⚠️

**ВНИМАНИЕ:** Это изменит данные в продакшн базе!

```sql
UPDATE users 
SET 
  "video1ShownAt" = "lastActivityAt",
  "video1ReminderSent" = false
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ShownAt" IS NULL;
```

**Вы увидите:**
```
UPDATE 32
```

Это значит что обновлено 32 пользователя.

### Шаг 5: Проверьте результаты

```sql
SELECT 
  COUNT(*) as total_video1_users,
  COUNT(CASE WHEN "video1ShownAt" IS NOT NULL THEN 1 END) as has_timestamp,
  COUNT(CASE WHEN "video1ShownAt" IS NULL THEN 1 END) as no_timestamp
FROM users 
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false;
```

**Должно быть:**
```
total_video1_users | has_timestamp | no_timestamp
-------------------|---------------|-------------
        32         |      32       |      0
```

Все пользователи на video1 должны иметь timestamp!

### Шаг 6: Проверьте кто получит напоминание сейчас

```sql
SELECT 
  COUNT(*) as will_receive_reminder_now
FROM users 
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ReminderSent" = false 
  AND "video1ShownAt" IS NOT NULL
  AND "video1ShownAt" <= NOW() - INTERVAL '10 minutes';
```

**Результат:**
```
will_receive_reminder_now
-------------------------
          28
```

Эти 28 пользователей получат напоминание при следующей проверке (через 1 минуту).

## 📊 Что произойдет дальше

### Через 1 минуту (следующая проверка):

В логах Railway появится:

```
📊 Найдено пользователей для напоминания (video1): 28
🔔 Отправка напоминания video1 пользователю 123456789
✅ Напоминание video1 отправлено пользователю 123456789
🔔 Отправка напоминания video1 пользователю 987654321
✅ Напоминание video1 отправлено пользователю 987654321
...
```

### Пользователи получат сообщение:

```
Нет времени смотреть видео? Понимаю, я тоже все время на бегу. 
Хочешь я просто сразу дам тебе ссылку на платный канал 
со всеми моими инструментами и инструкцией как пользоваться?

[✨ Хочу!]
```

## ⚠️ Возможные проблемы и решения

### Проблема 1: Слишком много пользователей получат сразу

**Если результат Шага 2 показывает >100 пользователей:**

Можно отправлять порциями:

```sql
-- Вариант A: Только самым свежим (застряли <1 часа назад)
UPDATE users 
SET "video1ShownAt" = "lastActivityAt"
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ShownAt" IS NULL
  AND "lastActivityAt" >= NOW() - INTERVAL '1 hour';

-- Потом через час обновить остальных
```

### Проблема 2: Не хотите флуд напоминаний

**Вариант: Задержать отправку**

Установите `video1ShownAt` на 5 минут назад (чтобы напоминание пришло через 5 минут):

```sql
UPDATE users 
SET "video1ShownAt" = NOW() - INTERVAL '5 minutes'
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false 
  AND "video1ShownAt" IS NULL;
```

Все получат напоминание через 5 минут одновременно.

## 🔙 ОТКАТ (если что-то пошло не так)

### Полный откат миграции:

```sql
UPDATE users 
SET 
  "video1ShownAt" = NULL,
  "video1ReminderSent" = false
WHERE "currentStep" = 'video1' 
  AND "hasPaid" = false;
```

### Частичный откат (только для конкретного пользователя):

```sql
UPDATE users 
SET 
  "video1ShownAt" = NULL,
  "video1ReminderSent" = false
WHERE "userId" = 123456789;
```

## 📈 Мониторинг результатов

### После миграции следите за:

1. **Логи Railway** - сколько напоминаний отправлено
2. **Команда /stats** - уменьшение `stuck_at_video1`
3. **Конверсия** - рост `payment_choice` и успешных оплат

### SQL для анализа эффективности:

```sql
-- Сколько пользователей после напоминания:
-- 1. Перешли на payment_choice
-- 2. Оплатили

SELECT 
  COUNT(*) as total_reminders_sent,
  COUNT(CASE WHEN "currentStep" != 'video1' THEN 1 END) as moved_forward,
  COUNT(CASE WHEN "hasPaid" = true THEN 1 END) as converted_to_paid,
  ROUND(100.0 * COUNT(CASE WHEN "hasPaid" = true THEN 1 END) / COUNT(*), 2) as conversion_rate
FROM users 
WHERE "video1ReminderSent" = true;
```

## ✅ Чек-лист выполнения

- [ ] Шаг 1: Открыл Railway PostgreSQL Query
- [ ] Шаг 2: Проверил количество пользователей (сколько будет обновлено)
- [ ] Шаг 3: Посмотрел примеры пользователей
- [ ] Шаг 4: **ВЫПОЛНИЛ UPDATE** (применил миграцию)
- [ ] Шаг 5: Проверил результаты (все имеют timestamp)
- [ ] Шаг 6: Проверил сколько получат напоминание сейчас
- [ ] Наблюдаю логи Railway (видны отправки напоминаний)
- [ ] Мониторю /stats (уменьшение stuck_at_video1)

## 🎯 Ожидаемый результат

**ДО миграции:**
```
📊 Найдено пользователей для напоминания (video1): 0
```

**ПОСЛЕ миграции:**
```
📊 Найдено пользователей для напоминания (video1): 28
🔔 Отправка напоминания video1 пользователю...
✅ Напоминание video1 отправлено...
```

**Через несколько часов в /stats:**
```
📹 Застряли на видео 1: 32 → 12 (улучшение на 62%!)
💳 На выборе оплаты: 20 → 35 (рост на 75%!)
✅ Оплатили: 45 → 52 (рост на 15%!)
```

---

**Создано:** 2 ноября 2025  
**Тип:** Разовая миграция данных  
**Безопасность:** Обратимо (есть откат)  
**Время выполнения:** <1 секунды для UPDATE
