# 🚀 Быстрый старт: Синхронизация канала

## Проблема, которую мы решаем

Jana Kin (@Kin_pmu) оплатила через EUR Tribute, но в базе данных она НЕ помечена как оплатившая (`hasPaid = false`). Это может происходить из-за:
- Webhook от Tribute не пришел
- Оплата прошла, но бот не обработал

**Решение:** Синхронизировать участников канала с базой данных - кто в канале = автоматически `hasPaid = true`

---

## ⚡ Вариант 1: Быстрая синхронизация (Bot API)

**Ограничение:** Синхронизирует только администраторов канала

```bash
# В Telegram отправьте боту команду:
/sync_channel
```

**Автоматика:**
- ✅ Запускается автоматически при старте бота
- ✅ Повторяется каждые 6 часов
- ⚠️ Синхронизирует только администраторов

---

## 🚀 Вариант 2: Полная синхронизация (Python MTProto) **РЕКОМЕНДУЕТСЯ**

Синхронизирует ВСЕХ участников канала, включая Jana Kin

### Шаг 1: Получите API credentials

1. Перейдите на https://my.telegram.org/apps
2. Войдите с вашим номером телефона
3. Нажмите "Create application"
4. Скопируйте `API ID` и `API Hash`

### Шаг 2: Настройте .env файл

Добавьте в `.env`:

```env
# Ваши существующие переменные...

# Telegram MTProto API (для полной синхронизации)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_PHONE=+79001234567
```

### Шаг 3: Установите Python зависимости

```bash
pip install telethon asyncpg python-dotenv
```

### Шаг 4: Запустите синхронизацию

```bash
cd "/Users/legend/Desktop/PRODIGY/apps venture/бот приема платежей"
python scripts/sync_channel_full.py
```

**Первый запуск:**
- Telegram отправит код подтверждения на ваш номер
- Введите код в консоли
- После этого credentials сохранятся

**Результат:**
```
🔍 Получаю участников канала -1003216850856...
  Загружено 50 участников...
  Загружено 100 участников...
  Загружено 150 участников...
✅ Всего найдено участников: 150

📊 Обрабатываю 150 участников...
  ✅ Jana Kin (260343060) помечен как оплативший
  ...

✅ Синхронизация завершена!

============================================================
📊 ОТЧЕТ О СИНХРОНИЗАЦИИ КАНАЛА
============================================================
👥 Всего участников: 150
✅ Известных пользователей: 120
💰 Помечено как оплативших: 15  ← Jana Kin здесь
✔️  Уже были оплачены: 105
👤 Новых friends добавлено: 30
============================================================
```

### Шаг 5: Проверьте результат

```bash
PGPASSWORD='tbswlvQbgFmiOdcJPKyAckRuSmvrYxxw' psql -h nozomi.proxy.rlwy.net -p 35365 -U postgres -d railway -c "SELECT \"userId\", username, \"firstName\", \"hasPaid\", \"paidAt\" FROM users WHERE username = 'Kin_pmu';"
```

**Ожидаемый результат:**
```
  userId   | username | firstName | hasPaid |          paidAt           
-----------+----------+-----------+---------+---------------------------
 260343060 | Kin_pmu  | Jana      | t       | 2025-11-06 12:34:56.789
```

---

## 📊 Что происходит при синхронизации

1. **Получаем всех участников канала** (Bot API: только админы, MTProto: все)
2. **Для каждого участника:**
   - Если есть в `users` таблице:
     - `hasPaid = false` → обновляем на `true`, ставим `paidAt`, `currentStep = 'completed'`
     - `hasPaid = true` → пропускаем
   - Если НЕТ в `users` таблице:
     - Добавляем в таблицу `friends` (бесплатный доступ, прямая ссылка и т.д.)

---

## 🔍 Проверка таблицы Friends

После синхронизации проверьте кто получил бесплатный доступ:

```sql
SELECT * FROM friends ORDER BY "addedAt" DESC LIMIT 20;
```

Это пользователи которые:
- В канале, но никогда не запускали бота
- Получили прямую ссылку на канал
- Бесплатный доступ от админа

---

## 🔄 Регулярное использование

**Рекомендация:**
- **Автоматически:** Bot API каждые 6 часов (уже настроено)
- **Вручную:** Python MTProto раз в неделю для полной проверки

**Cron (опционально):**
```bash
# Каждое воскресенье в 3:00 AM
0 3 * * 0 cd /path/to/bot && python3 scripts/sync_channel_full.py >> logs/sync.log 2>&1
```

---

## ⚠️ Важно

**НЕ КОММИТЬТЕ:**
- `channel_sync_session.session*` - файл авторизации
- API credentials в `.env`

Уже добавлено в `.gitignore` ✅

---

## 📖 Полная документация

См. `CHANNEL_SYNC_README.md` для детальной информации и troubleshooting.
