# 🔧 Troubleshooting: PostgreSQL Connection Issues

## Проблема: "Connection terminated unexpectedly"

Эта ошибка возникает когда TypeORM пытается подключиться к PostgreSQL на Railway, но соединение разрывается.

### Причины:

1. **SSL соединение** - Railway требует SSL для подключения к PostgreSQL
2. **Таймауты** - Создание новых колонок может занять время
3. **Пул соединений** - Нестабильное управление соединениями

## Решение

### 1. Добавлены настройки SSL

```typescript
ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false
```

### 2. Увеличены таймауты

```typescript
connectTimeoutMS: 30000,
extra: {
  max: 10,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 30000,
}
```

### 3. Добавлена логика повторных попыток

При неудачном подключении бот автоматически пытается переподключиться до 5 раз с экспоненциальной задержкой.

## Мониторинг

В логах Railway теперь будет:

```
🔄 Попытка подключения к БД... (1/5)
✅ База данных подключена успешно
✅ Тестовый запрос выполнен успешно
```

Или при ошибке:

```
🔄 Попытка подключения к БД... (1/5)
❌ Ошибка подключения к БД (попытка 1/5): Error: ...
⏳ Ожидание 2000ms перед следующей попыткой...
🔄 Попытка подключения к БД... (2/5)
✅ База данных подключена успешно
```

## Если проблема повторяется

### Проверьте переменные окружения на Railway:

1. `DATABASE_URL` - должна быть установлена автоматически
2. `NODE_ENV` - должна быть `production`

### Проверьте статус PostgreSQL:

В Railway Dashboard → PostgreSQL → Settings → убедитесь что база данных запущена.

### Ручное подключение для проверки:

Зайдите в Railway Dashboard → PostgreSQL → Data и попробуйте выполнить запрос:

```sql
SELECT * FROM users LIMIT 1;
```

Если запрос не выполняется, проблема в самой базе данных Railway, а не в боте.

## Альтернативное решение: Отключить synchronize

Если проблемы с подключением продолжаются, можно отключить автоматическую синхронизацию схемы:

В `src/database.ts`:

```typescript
synchronize: false, // Отключить автоматическое создание таблиц
```

**Но тогда нужно будет вручную создать новые колонки:**

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS "paymentChoiceShownAt" TIMESTAMP;
ALTER TABLE users ADD COLUMN IF NOT EXISTS "paymentReminderSent" BOOLEAN DEFAULT false;
ALTER TABLE users ADD COLUMN IF NOT EXISTS "waitingReceiptSince" TIMESTAMP;
ALTER TABLE users ADD COLUMN IF NOT EXISTS "receiptReminderSent" BOOLEAN DEFAULT false;
```

## Проверка версии PostgreSQL

Railway использует PostgreSQL 15+. Убедитесь что версия совместима:

```sql
SELECT version();
```

## Контакты для поддержки

- Railway Support: https://railway.app/help
- TypeORM Issues: https://github.com/typeorm/typeorm/issues
