# 🔥 Инструкция по запуску Warmup рассылки

## Шаг 1: Проверка текущего состояния

Отправь боту команду:
```
/stats
```

Это создаст первый snapshot и покажет текущее состояние:
- Сколько застряло на старте
- Сколько застряло на video1
- Текущие счетчики warmup (должны быть 0)

**Скриншот этой статистики сохрани!**

---

## Шаг 2: Запуск разовой warmup рассылки

Отправь боту команду:
```
/warmup_broadcast
```

Бот пришлёт отчёт:
```
🔥 РЕЗУЛЬТАТЫ WARMUP РАССЫЛКИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

👥 Всего пользователей: 156
✅ Отправлено: 153
❌ Ошибок: 3

📊 Теперь используй /stats чтобы отследить как они продвинутся по воронке!
```

---

## Шаг 3: Наблюдение за результатами

### Сразу после рассылки (через 30 секунд):
```
/stats
```

Должно показать в воронке:
```
🚫 Застряли на старте: 28 (🔥 28)
📹 Застряли на видео 1: 114 (📨 45) (🔥 114)
```

🔥 = warmup отправлен

---

### Через 5 минут:
```
/stats
```

В секции UPDATES увидишь:
```
📈 UPDATES (с момента последней проверки)
⏱ Прошло времени: 5м

👥 Новых пользователей: +0
✅ Новых оплат: +8
🔥 Warmup Start отправлено: +28
🔥 Warmup Video1 отправлено: +114

📊 Изменения в воронке:
  • Старт: -12 (двигаются дальше!)
  • Видео1: -23 (двигаются к оплате!)
  • Выбор оплаты: +18 (пришли после warmup!)
  • Ждут квитанции: +15 (уже оплатили!)
```

---

### Через 30 минут:
```
/stats
```

Должны увидеть итоговый прогресс:
```
✅ Новых оплат: +15-25 (из 142 получивших warmup)
```

Конверсия warmup обычно **10-17%** в течение первых 30 минут.

---

## Автоматический догрев

После разовой рассылки **автодогрев уже работает**!

Каждые 2 минуты бот автоматически:
1. Ищет пользователей на `start` (застряли 5+ минут, не получали warmup)
2. Ищет пользователей на `video1` (застряли 10+ минут, не получали warmup)
3. Отправляет им warmup сообщение
4. Ставит флаг чтобы не слать повторно

**Новые пользователи автоматически получат warmup!** 🔥

---

## Что делать дальше

1. **Каждый час** проверяй `/stats` чтобы видеть прогресс
2. **Через 24 часа** оцени общую эффективность warmup
3. **Экспериментируй** с текстом сообщения (в `src/services/warmupService.ts`)

---

## Логи на Railway

Чтобы видеть в реальном времени когда отправляются warmup:

1. Зайди на Railway.app
2. Открой логи бота
3. Ищи строки:
```
🔥 Warmup: найдено 3 на start, 7 на video1
✅ Warmup отправлен пользователю 123456 (start)
✅ Warmup отправлен пользователю 789012 (video1)
```

---

## Готово! 🚀

Система warmup работает автоматически, дополнительных действий не требуется.
