# 🔍 Проверка ошибки генерации invite ссылок

## Что проверить:

### 1. Логи Railway
Откройте Railway Dashboard → Deployments → View Logs

Ищите строки с ошибками:
```
Error generating invite links
Error creating invite link
Bot is not an administrator
```

### 2. Возможные причины ошибки:

#### A) Бот не добавлен в канал/чат
- Канал ID: -1003216850856
- Чат ID: -1002895096401

**Решение:**
1. Добавьте бота @Reels_sale_bot в канал
2. Добавьте бота @Reels_sale_bot в чат
3. Сделайте бота администратором в обоих

#### B) У бота нет прав на создание invite ссылок

**Решение:**
1. Откройте канал/чат
2. Найдите бота в списке администраторов
3. Включите право "Invite Users via Link" (создание пригласительных ссылок)

#### C) Неверные ID канала/чата

**Проверка:**
```bash
# В Railway или локально
echo $CHANNEL_ID
echo $CHAT_ID
```

Должно быть:
- CHANNEL_ID=-1003216850856
- CHAT_ID=-1002895096401

### 3. Быстрая проверка прав бота

Отправьте боту тестовое сообщение и проверьте может ли он создать ссылку.

Или используйте Telegram Bot API напрямую:

```bash
# Проверка статуса бота в канале
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getChatMember?chat_id=-1003216850856&user_id=<BOT_USER_ID>"

# Попытка создать invite ссылку
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/createChatInviteLink?chat_id=-1003216850856&member_limit=1"
```

### 4. Пошаговое решение:

#### Шаг 1: Проверьте бота в канале
1. Откройте канал в Telegram
2. Перейдите в настройки → Администраторы
3. Найдите @Reels_sale_bot
4. Если нет - добавьте как администратора

#### Шаг 2: Проверьте права
При добавлении бота включите:
- ✅ **Invite Users via Link** (обязательно!)
- ✅ Manage Chat (желательно)
- ✅ Delete Messages (опционально)

#### Шаг 3: Повторите для чата
То же самое для чата с ID -1002895096401

#### Шаг 4: Проверьте в коде
Файл: src/index.ts строки 750 и 783

```typescript
// Создание ссылки на канал
const inviteLink = await bot.telegram.createChatInviteLink(config.channelId, {
  member_limit: 1,
  expire_date: Math.floor(Date.now() / 1000) + 86400
});

// Создание ссылки на чат
const inviteLink = await bot.telegram.createChatInviteLink(config.chatId, {
  member_limit: 1,
  expire_date: Math.floor(Date.now() / 1000) + 86400
});
```

### 5. Типичные ошибки и их решения:

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Bot is not an administrator` | Бот не админ | Сделайте бота администратором |
| `Bot was kicked from the channel` | Бот удален | Добавьте бота заново |
| `Chat not found` | Неверный ID | Проверьте CHANNEL_ID и CHAT_ID |
| `Bad Request: not enough rights` | Нет прав | Включите "Invite Users via Link" |
| `Forbidden` | Бот заблокирован | Разблокируйте и добавьте заново |

### 6. Как получить правильные ID:

#### Для канала:
1. Добавьте @userinfobot в канал
2. Отправьте любое сообщение
3. Бот покажет ID канала (начинается с -100...)

#### Для чата:
То же самое - добавьте @userinfobot в чат

### 7. Тестирование после исправления:

1. Отправьте квитанцию боту
2. Если квитанция валидна, бот должен:
   - ✅ Сгенерировать ссылку на канал
   - ✅ Сгенерировать ссылку на чат
   - ✅ Отправить обе ссылки пользователю

Формат сообщения:
```
🎉 Поздравляем!

📺 Доступ к каналу с материалами:
https://t.me/+xxxxxxxxxxxxx

💬 Доступ к чату с сообществом:
https://t.me/+xxxxxxxxxxxxx

⏰ Ссылки действительны 24 часа
👤 Каждая ссылка может быть использована только один раз

Добро пожаловать в наше сообщество! 🚀
```

### 8. Если ошибка повторяется:

Проверьте логи Railway и отправьте мне точный текст ошибки. Там будет написано:
- `Error generating invite links: <точная причина>`
- `Error checking bot status: <детали>`

---

## Быстрый чеклист:

- [ ] Бот добавлен в канал (-1003216850856) как администратор
- [ ] Бот добавлен в чат (-1002895096401) как администратор
- [ ] У бота есть право "Invite Users via Link" в канале
- [ ] У бота есть право "Invite Users via Link" в чате
- [ ] CHANNEL_ID в .env = -1003216850856
- [ ] CHAT_ID в .env = -1002895096401
- [ ] Railway переменные окружения совпадают с локальными

---

**Самая частая причина:** Бот не является администратором или не имеет права создавать invite ссылки!
