# Синхронизация участников канала с базой данных

## Зачем это нужно?

Система автоматически отслеживает пользователей, которые оплатили доступ через Telegram Tribute или загрузили квитанцию. Однако, синхронизация с каналом позволяет:

1. **Пометить как оплативших** всех пользователей, которые уже находятся в канале (даже если оплата прошла через Tribute, но webhook не сработал)
2. **Отследить "friends"** - пользователей, которых добавили в канал вручную без бота (бесплатный доступ, промо и т.д.)

## Два способа синхронизации

### Способ 1: Bot API (только администраторы) ⚡

**Преимущества:**
- Простой, работает из коробки
- Не требует дополнительной настройки
- Автоматически запускается каждые 6 часов

**Ограничения:**
- Telegram Bot API предоставляет только список администраторов канала
- НЕ получает полный список участников

**Использование:**
```bash
# Команда в боте (только для админа)
/sync_channel
```

Синхронизация также запускается автоматически:
- При старте бота
- Каждые 6 часов

---

### Способ 2: MTProto API (все участники) 🚀 **РЕКОМЕНДУЕТСЯ**

**Преимущества:**
- Получает ПОЛНЫЙ список всех участников канала
- Точная синхронизация всех оплат

**Требования:**
1. Python 3.8+
2. API credentials от Telegram

**Установка:**

1. Получите API credentials:
   - Перейдите на https://my.telegram.org/apps
   - Войдите с вашим номером телефона
   - Создайте приложение и получите `API_ID` и `API_HASH`

2. Добавьте в `.env` файл:
   ```env
   TELEGRAM_API_ID=ваш_api_id
   TELEGRAM_API_HASH=ваш_api_hash
   TELEGRAM_PHONE=+ваш_номер_телефона
   ```

3. Установите Python зависимости:
   ```bash
   pip install telethon asyncpg python-dotenv
   ```

4. Запустите скрипт:
   ```bash
   python scripts/sync_channel_full.py
   ```

**Первый запуск:**
- Телеграм отправит код подтверждения на ваш номер
- Введите код в консоли
- Credentials сохранятся в файле `channel_sync_session.session`

**Последующие запуски:**
- Просто запускайте скрипт, авторизация не потребуется

---

## Что происходит при синхронизации

1. **Получение списка участников канала**
   - Bot API: только администраторы
   - MTProto API: все участники

2. **Проверка каждого участника:**
   - Если пользователь есть в таблице `users`:
     - `hasPaid = false` → помечаем `hasPaid = true`, ставим `paidAt = NOW()`, `currentStep = 'completed'`
     - `hasPaid = true` → пропускаем (уже оплачен)
   - Если пользователя нет в таблице `users`:
     - Добавляем в таблицу `friends` с пометкой "Добавлен через синхронизацию канала"

3. **Отчет:**
   ```
   📊 ОТЧЕТ О СИНХРОНИЗАЦИИ КАНАЛА
   =============================================
   👥 Всего участников: 150
   ✅ Известных пользователей: 120
   💰 Помечено как оплативших: 15
   ✔️  Уже были оплачены: 105
   👤 Новых friends добавлено: 30
   ```

---

## Таблица Friends

Новые пользователи, найденные в канале, но не зарегистрированные в боте, сохраняются в таблицу `friends`:

```sql
SELECT * FROM friends;
```

| userId | username | firstName | lastName | addedAt | notes |
|--------|----------|-----------|----------|---------|-------|
| 123... | @user1   | John      | Doe      | 2025-11-06 | Добавлен через синхронизацию |

**Это могут быть:**
- Люди, которым дали прямую ссылку на канал
- Бесплатные доступы для партнеров
- Тестовые аккаунты

---

## Рекомендации

### Первый запуск:
1. Используйте **MTProto скрипт** для полной синхронизации всех участников
2. Проверьте результаты:
   ```sql
   SELECT COUNT(*) FROM users WHERE "hasPaid" = true;
   SELECT COUNT(*) FROM friends;
   ```

### Регулярное использование:
- **Автоматическая синхронизация Bot API** (каждые 6 часов) для администраторов
- **Ручной запуск MTProto скрипта** раз в неделю для полной проверки

### Проверка конкретного пользователя:
```bash
# В боте
/stats

# В PostgreSQL
SELECT "userId", username, "firstName", "hasPaid", "paidAt" 
FROM users 
WHERE username = 'kin_pmu';
```

---

## Troubleshooting

### "Cannot find module 'telethon'"
```bash
pip install telethon asyncpg python-dotenv
```

### "API_ID must be an integer"
Проверьте `.env` файл:
```env
TELEGRAM_API_ID=12345678  # число, без кавычек
TELEGRAM_API_HASH=abcdef123456...  # строка
```

### "FloodWaitError"
Telegram временно ограничил запросы. Подождите указанное время.

### "SessionPasswordNeededError"
У вашего аккаунта включена двухфакторная аутентификация. Введите пароль при запросе.

---

## Безопасность

⚠️ **НЕ КОММИТЬТЕ:**
- `channel_sync_session.session` - файл с авторизацией
- API credentials в `.env`

Добавлено в `.gitignore`:
```
channel_sync_session.session*
.env
```

---

## Cron автоматизация (опционально)

Для автоматического запуска раз в неделю:

```bash
# Добавить в crontab
crontab -e

# Каждое воскресенье в 3:00 AM
0 3 * * 0 cd /path/to/bot && /usr/bin/python3 scripts/sync_channel_full.py >> logs/sync.log 2>&1
```
