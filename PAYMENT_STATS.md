# 📊 Статистика платежей

## Обзор

В базе данных PostgreSQL на Railway теперь есть **VIEW** (представление) `payment_stats`, которое автоматически агрегирует статистику по всем платежам и действиям пользователей.

## Структура данных

### Таблица `payment_stats` (VIEW)

Это **не обычная таблица**, а **представление** - виртуальная таблица, которая автоматически пересчитывает данные в реальном времени на основе таблиц `users` и `user_actions`.

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | integer | Всегда равен 1 (для совместимости с ORM) |
| `total_users_started` | integer | Сколько уникальных пользователей запустили бота (/start) |
| `total_successful_payments` | integer | Сколько успешно принято оплат (hasPaid = true) |
| `total_rub_payments` | integer | Сколько платежей в рублях |
| `total_uah_payments` | integer | Сколько платежей в гривнах |
| `total_non_receipts` | integer | Сколько раз прислали "не квитанцию" (action = 'photo_rejected') |
| `total_failed_receipts` | integer | Сколько квитанций не прошли проверку (action = 'receipt_validation_failed') |

## Как просмотреть статистику

### 1. В Telegram боте (для админа)

Отправьте команду:
```
/stats
```

Вы получите красиво отформатированное сообщение со всей статистикой.

### 2. В Railway Dashboard

1. Откройте ваш проект на Railway
2. Перейдите в PostgreSQL → Data
3. Найдите VIEW `payment_stats`
4. Кликните на него чтобы увидеть актуальные данные

### 3. В консоли при запуске бота

При каждом запуске бота в логах Railway будет автоматически выводиться статистика:

```
📊 СТАТИСТИКА ПЛАТЕЖЕЙ:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 Всего уникальных пользователей: 150
✅ Успешных оплат: 45
💵 Оплат в рублях: 30
💴 Оплат в гривнах: 15
📷 Отправлено "не квитанций": 12
❌ Квитанций не прошедших проверку: 8
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 4. SQL запрос (PostgreSQL консоль)

Если хотите вручную посмотреть данные:

```sql
SELECT * FROM payment_stats;
```

## Как работает VIEW

VIEW `payment_stats` - это **виртуальная таблица**, которая не хранит данные физически. Вместо этого она выполняет SQL запрос каждый раз, когда вы к ней обращаетесь, и возвращает актуальные данные.

**SQL запрос VIEW:**
```sql
SELECT 
  1 as id,
  COUNT(DISTINCT u.userId) as total_users_started,
  COUNT(DISTINCT CASE WHEN u.hasPaid = true THEN u.userId END) as total_successful_payments,
  COUNT(DISTINCT CASE WHEN u.currency = 'RUB' AND u.hasPaid = true THEN u.userId END) as total_rub_payments,
  COUNT(DISTINCT CASE WHEN u.currency = 'UAH' AND u.hasPaid = true THEN u.userId END) as total_uah_payments,
  COUNT(CASE WHEN ua.action = 'photo_rejected' THEN 1 END) as total_non_receipts,
  COUNT(CASE WHEN ua.action = 'receipt_validation_failed' THEN 1 END) as total_failed_receipts
FROM users u
LEFT JOIN user_actions ua ON u.userId = ua.userId
```

## Преимущества использования VIEW

✅ **Всегда актуальные данные** - не нужно обновлять статистику вручную  
✅ **Не занимает место** - данные не дублируются  
✅ **Автоматический пересчет** - при изменении данных в `users` или `user_actions` статистика обновляется автоматически  
✅ **Простой доступ** - можно запросить как обычную таблицу  

## Техническая реализация

Созданы файлы:
- `src/entities/PaymentStats.ts` - TypeORM Entity для VIEW
- `src/statsService.ts` - Сервис для работы со статистикой
- `src/database.ts` - Зарегистрирован новый Entity
- `src/index.ts` - Добавлена команда `/stats` и вывод при старте

## Примечания

- Доступ к команде `/stats` имеет только админ (userId: 278263484)
- VIEW создается автоматически при первом запуске бота после деплоя
- Если VIEW не создался, проверьте логи Railway на наличие ошибок подключения к БД
